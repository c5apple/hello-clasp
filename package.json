{
  "scripts": {
    "build": "node esbuild.mjs",
    "push": "clasp push && clasp version",
    "deploy": "npm run build && npm run push"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.84",
    "esbuild": "^0.24.0",
    "esbuild-gas-plugin": "^0.8.0",
    "typescript": "^5.6.3"
  }
}
